import React from "react";
import { TwitterPicker } from "react-color";

const EditorProps = () => {
    return (
        <div>
            <TwitterPicker />
        </div>
    );
};
export default EditorProps;
